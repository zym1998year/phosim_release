# CMake build directories
build/
build-*/

# Compiled binaries are produced into bin/ by the CMake build; bin/setup
# is generated by configure. Per-platform binary names are already handled
# by bin/.gitignore (which ignores '*' but tracks .gitignore itself).
